{
  "name": "chain-builders-digest-feed",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Centralized X feed for Chain Builders Digest — runs in GitHub Actions with official X API v2",
  "scripts": {
    "check": "node --check scripts/generate-feed.js",
    "generate": "node scripts/generate-feed.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
